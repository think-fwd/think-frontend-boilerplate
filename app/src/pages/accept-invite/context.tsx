import { createContext } from 'use-context-selector'
import { AcceptInvitePageContextProps } from './types'

export const AcceptInvitePageContext = createContext(
  {} as AcceptInvitePageContextProps
)
